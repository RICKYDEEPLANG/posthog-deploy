global:
  resolve_timeout: 5m

route:
  receiver: 'feishu'
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 12h

receivers:
  - name: 'feishu'
    webhook_configs:
      - url: 'http://feishu-alert-adapter:8080/webhook'
        send_resolved: true
